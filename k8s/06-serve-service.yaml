apiVersion: v1
kind: Service
metadata:
  name: fraud-serving-svc
  namespace: fraud-detection
spec:
  selector:
    app: fraud-serving
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
